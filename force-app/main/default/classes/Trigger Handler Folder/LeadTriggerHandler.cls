public class LeadTriggerHandler {
  // When duplicates lead are found based on email,
  // re-parent all the child records to the oldest record (remove all the tasks from other records) and
  // delete the duplicate records apart from the oldest record.

  public static void reparentChildsIfParentDuplicate() {
  }
}
